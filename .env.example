# HelioMetric Environment Variables
# Copy this file to .env.local and fill in your values

# =============================================================================
# UPSTASH REDIS (Required for caching)
# =============================================================================
# Get these from https://console.upstash.com/redis
# Used for caching NOAA data, geocoding results, and expensive calculations

UPSTASH_REDIS_REST_URL=https://your-redis-url.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-redis-token

# =============================================================================
# GOOGLE MAPS API (Required for location features)
# =============================================================================
# Get this from https://console.cloud.google.com/apis/credentials
# Enable: Geocoding API, Time Zone API

GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# =============================================================================
# RALPH AGENT MONITORING (Required for centralized monitoring)
# =============================================================================
# Enables bidirectional communication with Ralph Agent for:
# - Error tracking and alerting
# - Log fetching and analysis
# - Health monitoring
# - Metrics collection

# Ralph Agent URL (default: https://ralph-agent.onrender.com)
RALPH_MONITOR_URL=https://ralph-agent.onrender.com

# Shared secret for HMAC authentication (MUST match Ralph Agent's secret)
RALPH_MONITOR_SECRET=your-shared-secret

# Project identifier (use: helio-metric)
RALPH_PROJECT_ID=helio-metric

# Your application's callback URL for receiving Ralph requests
# Format: https://your-app-url.com/api/ralph-callback
MY_CALLBACK_URL=https://your-heliometric-url.onrender.com/api/ralph-callback

# =============================================================================
# RESEARCH AGENT (Required for research API endpoints)
# =============================================================================
# API key for authenticating research endpoint requests.
# All /api/research/* endpoints require this key via X-Research-Key header.
RESEARCH_API_KEY=your-research-api-key

# Anthropic API key for the research agent's Claude integration
ANTHROPIC_API_KEY=your-anthropic-api-key

# =============================================================================
# Optional Configuration
# =============================================================================

# Node environment (automatically set by hosting platform)
# NODE_ENV=production

# Production origin for CORS (recommended for security)
# PRODUCTION_ORIGIN=https://your-production-domain.com
